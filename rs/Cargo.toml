[package]
name = "cairn"
version = "0.1.0"
edition = "2021"
description = "Cairn append-only event store — Rust SDK"
license = "MIT"

[dependencies]
rusqlite = { version = "0.38", features = ["bundled"] }

[dev-dependencies]
serde_json = "1"
base64 = "0.22"
tempfile = "3"
